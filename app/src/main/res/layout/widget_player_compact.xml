<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:clipToOutline="true">

    <ImageView
        android:id="@+id/widget_bg_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/widget_bg_scrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#B31C1E23"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="10dp">

        <ImageView
            android:id="@+id/widget_artwork"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/widget_artwork_background"
            android:contentDescription="@string/widget_artwork"
            android:scaleType="centerCrop"
            android:src="@drawable/pailer_logo" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="10dp"
            android:gravity="center_vertical"
            android:orientation="vertical"
            android:layout_weight="1">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="sans"
                android:maxLines="1"
                android:text="Pailer FM"
                android:textColor="#FFF0E1CF"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/widget_artist"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:fontFamily="sans"
                android:maxLines="1"
                android:text="Biblioteca local"
                android:textColor="#CCFFFFFF"
                android:textSize="12sp" />

            <ProgressBar
                android:id="@+id/widget_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:layout_marginTop="8dp"
                android:max="1000"
                android:progress="0"
                android:progressDrawable="@drawable/widget_progress" />
        </LinearLayout>

        <ImageView
            android:id="@+id/widget_previous"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="@drawable/widget_control_background"
            android:clickable="true"
            android:contentDescription="@string/widget_previous"
            android:focusable="true"
            android:padding="8dp"
            android:scaleType="center"
            android:src="@drawable/ic_widget_previous" />

        <ImageView
            android:id="@+id/widget_play_pause"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="7dp"
            android:layout_marginEnd="7dp"
            android:background="@drawable/widget_play_background"
            android:clickable="true"
            android:contentDescription="@string/widget_play_pause"
            android:focusable="true"
            android:padding="9dp"
            android:scaleType="center"
            android:src="@drawable/ic_widget_play" />

        <ImageView
            android:id="@+id/widget_next"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="@drawable/widget_control_background"
            android:clickable="true"
            android:contentDescription="@string/widget_next"
            android:focusable="true"
            android:padding="8dp"
            android:scaleType="center"
            android:src="@drawable/ic_widget_next" />
    </LinearLayout>
</FrameLayout>
